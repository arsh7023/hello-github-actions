name: first

on:
  push:

jobs:
  job1:
    name: First Job
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Step One
        run: |
          env | sort
          echo First Job. Step One



